# Create a cache environment to store settings for project and system

Create a cache environment to store settings for project and system

## Usage

``` r
create_cache(project, verbose = FALSE)
```

## Arguments

- project:

  luna project object

- verbose:

  verbose output?
