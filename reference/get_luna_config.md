# Get luna config, either from global config file, or from project config

Get luna config, either from global config file, or from project config

## Usage

``` r
get_luna_config(verbose = TRUE)
```

## Arguments

- verbose:

  verbose output?
