# Apply column width truncation based on console width and relative width specs

Apply column width truncation based on console width and relative width
specs

## Usage

``` r
truncate_columns(df, width_specs, total_width = NULL)
```
