# Get a random sequence of letters and numbers of size `n`

Get a random sequence of letters and numbers of size `n`

## Usage

``` r
get_random_id(prefix = NULL, n = 6)
```

## Arguments

- prefix:

  optional prefix

## Value

character
