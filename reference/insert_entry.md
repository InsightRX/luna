# The reverse of pluck_entry, insert_entry() inserts an entry into an unnamded outer list, based on an element in the inner list.

The reverse of pluck_entry, insert_entry() inserts an entry into an
unnamded outer list, based on an element in the inner list.

## Usage

``` r
insert_entry(x, id, entry, el = "id")
```
