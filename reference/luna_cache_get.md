# Get element from active project Just a convenience wrapper with error checking

Get element from active project Just a convenience wrapper with error
checking

## Usage

``` r
luna_cache_get(entry = c("name", "folder"))
```
