# List runs in current project

List runs in current project

## Usage

``` r
luna_list(raw = FALSE)
```

## Arguments

- raw:

  if `TRUE`, will return a tibble with all information this is more
  useful for exporting to a file rather than viewing in console.
