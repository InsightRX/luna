# Read tables for a model and parse into an Xpose database

Read tables for a model and parse into an Xpose database

## Usage

``` r
luna_xpose(id, folder = NULL, verbose = TRUE)
```

## Arguments

- id:

  run id

- folder:

  folder

- verbose:

  verbose output?
