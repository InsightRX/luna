# helper function to extract all fit results based on vector of model files

helper function to extract all fit results based on vector of model
files

## Usage

``` r
get_all_results(model_file)
```
