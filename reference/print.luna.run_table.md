# Prints a list of models / runs in project

Prints a list of models / runs in project

## Usage

``` r
# S3 method for class 'luna.run_table'
print(x, format = "simple", verbose = FALSE, ...)
```

## Arguments

- x:

  luna project object
