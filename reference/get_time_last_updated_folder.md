# Get a timestamp for when the last update was made to any file in a folder Will look only 1 level deep.

Get a timestamp for when the last update was made to any file in a
folder Will look only 1 level deep.

## Usage

``` r
get_time_last_updated_folder(folder)
```

## Value

datetime stamp in ISO8601 format. Returns empty string if no files in
folder
