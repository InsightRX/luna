# Convert runs in a project object to a data.frame

Convert runs in a project object to a data.frame

## Usage

``` r
runs_as_table(x, raw = FALSE)
```

## Arguments

- x:

  data.frame with model info

- raw:

  if `TRUE`, will not parse data but just return all raw data
