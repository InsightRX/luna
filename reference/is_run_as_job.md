# Checks config if should run as job

Checks config if should run as job

## Usage

``` r
is_run_as_job(config, as_job = NULL)
```

## Arguments

- config:

  config list

- as_job:

  boolean, manual override over config
