# Save a project to a YAML file

Save a project to a YAML file

## Usage

``` r
save_project(project, path, verbose = FALSE, force = FALSE)
```

## Arguments

- project:

  uno project object

- path:

  path to file to save project

- verbose:

  verbosity

- force:

  force saving of the file, even if it already exists
